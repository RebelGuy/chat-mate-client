package dev.rebel.chatmate.gui.Interactive;

/** Lays out the added elements in an inline container. */
public class InlineElement extends ContainerElement {
  public InlineElement(InteractiveScreen.InteractiveContext context, IElement parent) {
    super(context, parent, LayoutMode.INLINE);
  }
}
