package dev.rebel.chatmate.services;

import dev.rebel.chatmate.gui.ContextMenuStore;
import dev.rebel.chatmate.gui.models.Dim;

public class ContextMenuService {
  private final ContextMenuStore store;

  public ContextMenuService(ContextMenuStore store) {
    this.store = store;
  }

  // todo: add several version of this with various types of data input data
  public void showContext(Dim x, Dim y) {

  }
}
